import { asset } from './asset';

/** Downloadable CV — the PDF lives in /public and is exported from the source .docx. */
export const CV_FILENAME = 'Keean-Ferreira-CV.pdf';
export const CV_URL = asset(CV_FILENAME);
